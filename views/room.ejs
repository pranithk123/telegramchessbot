<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
<title>Chess Game</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>

<style>
body { background:#181818; color:#fff; font-family:sans-serif; display:flex; flex-direction:column; align-items:center; justify-content:center; height:100vh; margin:0; overflow:hidden; }
.hidden { display:none !important; }

/* BOARD */
.chessboard { width: 340px; height: 340px; position: relative; box-shadow: 0 0 15px #000; margin-top: 10px; }
.square { width: 42.5px; height: 42.5px; position: absolute; display: flex; align-items: center; justify-content: center; }
.light { background:#ebecd0; }
.dark { background:#779556; }
.piece { width:42.5px; height:42.5px; position:absolute; top:0; left:0; pointer-events:none; z-index:10; }
.piece-img { width:40px; height:40px; transition: transform 0.2s ease; }

/* FLIPPED LOGIC: We only rotate the pieces, not the whole board container */
.flipped .piece-img { transform: rotate(180deg); }

/* HIGHLIGHTS */
.selected { background: rgba(255, 255, 0, 0.5); }
.dot::after { content:""; width:12px; height:12px; background:rgba(0,0,0,0.2); border-radius:50%; }
.capture::after { content:""; width:38px; height:38px; border:4px solid rgba(0,0,0,0.2); border-radius:50%; }

/* MODAL */
#setup-modal { position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 100; display: flex; align-items: center; justify-content: center; }
.modal-content { background: #2a2a2a; padding: 20px; border-radius: 15px; width: 85%; max-width: 320px; text-align: center; box-shadow: 0 0 20px #000; }
.slider-container { margin: 20px 0; }
input[type=range] { width: 100%; accent-color: #779556; }
.btn-row { display: flex; gap: 10px; justify-content: center; margin-top: 20px; }
.choice-btn { padding: 12px 24px; border: none; border-radius: 8px; font-weight: bold; font-size:16px; cursor: pointer; }
.btn-white { background: #ebecd0; color: #000; }
.btn-black { background: #333; color: #fff; border: 1px solid #555; }

/* UI ELEMENTS */
.timer-bar { display: flex; justify-content: space-between; width: 340px; margin: 5px 0; }
.timer { background: #333; padding: 5px 12px; border-radius: 5px; font-family: monospace; font-size: 16px; }
.player-info { display:flex; align-items:center; gap:10px; font-size:14px; color:#ccc; }
</style>
</head>

<body>

<div id="setup-modal" class="hidden">
  <div class="modal-content">
    <h3>⚙️ Setup Game</h3>
    <div class="slider-container">
      <label>Time Control: <span id="time-display" style="font-weight:bold; color:#779556;">10</span> min</label><br><br>
      <input type="range" id="time-slider" min="1" max="30" value="10" 
             oninput="document.getElementById('time-display').innerText = this.value">
    </div>
    <p>Choose your side:</p>
    <div class="btn-row">
      <button class="choice-btn btn-white" onclick="confirmSettings('w')">White</button>
      <button class="choice-btn btn-black" onclick="confirmSettings('b')">Black</button>
    </div>
  </div>
</div>

<div id="waiting" class="hidden" style="text-align:center;">
  <h2>Waiting for opponent...</h2>
  <p style="color:#888;">Forward the message in Telegram to invite a friend!</p>
</div>

<div id="game" class="hidden">
  <div class="timer-bar">
    <div class="player-info">👤 <span id="top-name">Opponent</span></div>
    <div id="top-timer" class="timer">10:00</div>
  </div>

  <div class="chessboard"></div>

  <div class="timer-bar">
    <div class="player-info">👤 <span id="bot-name">You</span></div>
    <div id="bottom-timer" class="timer">10:00</div>
  </div>
</div>

<script src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
<script>const ROOM_ID = "<%= roomId %>";</script>
<script src="/js/chessgame.js"></script>
</body>
</html>